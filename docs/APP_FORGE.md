# App Forge

Recursive create layer for All Gas: **apps that create apps**.

## Judge demo
1. Open https://quirky-rhinoceros-204.convex.site/
2. Use CeilingGate Demo signature (GRANT → $1 REFUSE)
3. Scroll to **App Forge** → run live Tip Jar / Line Delta / Mask Chip gates
4. Spawn a micro-app (Convex) · on box: `npm run forge -- --slug <slug> --title "…" --brief "…"` → `/workspace/forged-apps/<slug>`

## LIVE interactive children
`tip-jar-honesty`, `line-delta-kit`, `mask-chip-lite` — fully interactive ResidualGates (`npm run demo:gate` / `src/index.html`).
BETTER-THAN-PARENT on narrow claims. NEVER NEED ACCESS. See `docs/NO_COSTUME.md`.

## Stack
Same required artifacts: Convex + Firecrawl + AgentMail + ResidualGates honesty where spend applies.

## Cascading access
Every spawned app inherits STANDING_ACCESS / FREE CREATE fences (OFF PHONE, no sockpuppets, no Square commercial, silent to Taylor via MANAGER).
Nobody asks to create or interact.

## Originality
Forensic/create factory — **not** AgentMail chat-demo rebrand, not inbox yes/no.

## Public docs seeds
Prefer `https://docs.convex.dev/` (not www.convex.dev/documentation — 404). See `docs/FORGE_PUBLIC_URL_SEEDS_LIVE.md`.

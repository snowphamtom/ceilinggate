# HARD RULES (Taylor → MANAGER → Create) — cascade

1. **NO COSTUMES** — every shipped thing fully interactive/live (real clicks → real gates/state). No `App stub.txt`. No “ResidualGates stub” wording. No pretty-only UI.
2. **BETTER-THAN-PARENT** — every Forge child / feature must be strictly better than what spawned it (more useful, clearer gate, more real stack use) on its narrow claim.
3. **NEVER NEED ACCESS** — nobody asks to create or interact. Cascading self-authorize under OFF PHONE / no Square commercial / no sockpuppets / no fake $.

## LIVE children (proof)
- Tip Jar Honesty: `/forge/tip-jar-honesty/` — two money inputs, live-on-open, Demo GRANT/REFUSE, Δ + plain English + mask.
- Line Delta Kit: `/forge/line-delta-kit/` — paste arrays → live line table + GRANT/REFUSE + mask.
- Mask Chip Lite / Receipt Line Check: `/forge/mask-chip-lite/` · `/forge/receipt-line-check/`

Parent: https://quirky-rhinoceros-204.convex.site/

Fee ≠ prize. Silent to Taylor via MANAGER only.

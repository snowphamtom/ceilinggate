# BETTER-THAN-PARENT (Forge children)

Tip Jar Honesty and Line Delta Kit must each be **strictly better** than the parent CeilingGate demo on their narrow claim:

| Child | Narrow claim | Why better than parent |
|-------|--------------|------------------------|
| Tip Jar Honesty | claimed tip ≤ receipt total | Two labeled money inputs; one-click GRANT/REFUSE; no ship-set browsing required |
| Line Delta Kit | claimed[] vs interior[] | Paste arrays; shows mask + failed indices immediately |

Same `gateB` math as parent — clearer UI, live interactivity, never costume.
See also `docs/NO_COSTUME.md`.

# BETTER-THAN-PARENT (Forge children)

Every Forge child must be **strictly better** than the parent CeilingGate demo on its narrow claim:

| Child | Narrow claim | Why better than parent |
|-------|--------------|------------------------|
| Tip Jar Honesty | claimed tip ≤ receipt total | Two labeled money inputs; one-click GRANT/REFUSE; no ship-set browsing |
| Line Delta Kit | claimed[] vs interior[] | Paste arrays; line table + plain overage summary |
| Mask Chip Lite | failed-line mask bits | Interactive b0..bn chips (bit = line index) — parent mask is display-only |
| Receipt Line Check | one labeled line | Single line claimed vs on-receipt + Δ — faster than multi-line board |
| URL Receipt Gate | public URL → scrape → gate | Fetches real public receipt HTML; forensic ledger + plain English — not Attest yes/no chat |

Same `gateB` math as parent — clearer UI, live interactivity, never costume.
Empty-on-load + Reset required (Streamer). Demo flips ≠ win-proof.
See also `docs/NO_COSTUME.md`.

# Frontend (filled by `npx convex dev`)
VITE_CONVEX_URL=

# AgentMail Create inbox (public address — not a secret)
AGENTMAIL_INBOX_EMAIL=ceilinggate-claims@agentmail.to
# npx convex env set AGENTMAIL_INBOX_ID <id-from-create>
# npx convex env set AGENTMAIL_INBOX_EMAIL ceilinggate-claims@agentmail.to

# Set on the Convex deployment (never commit real secrets):
# npx convex env set CONVEX_DEPLOY_KEY ...
# npx convex env set FIRECRAWL_API_KEY ...
# npx convex env set FIRECRAWL_WEBHOOK_SECRET ...
# npx convex env set AGENTMAIL_API_KEY ...
# npx convex env set AGENTMAIL_WEBHOOK_SECRET ...

# Frontend (filled by `npx convex dev`)
VITE_CONVEX_URL=

# Set on the Convex deployment (never commit real values):
# npx convex env set AGENTMAIL_API_KEY ...
# npx convex env set AGENTMAIL_WEBHOOK_SECRET ...
# npx convex env set FIRECRAWL_API_KEY ...
# npx convex env set FIRECRAWL_WEBHOOK_SECRET ...

# AgentMail (created 2026-09-05)
AGENTMAIL_INBOX_ID=ceilinggate@agentmail.to
AGENTMAIL_INBOX_EMAIL=ceilinggate@agentmail.to
